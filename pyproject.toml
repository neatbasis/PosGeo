[project]
name = "posgeo"
version = "0.1.0"
description = "Minimal positive-geometry canonical-form playground (Option A)"
requires-python = ">=3.10"
dependencies = ["sympy>=1.12", "pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]
